# Optional in development because Vite proxies /api to port 3001.
# Set this only when the backend is deployed on a different origin.
VITE_API_URL=

# Google OAuth client ID for "Sign in with Google".
# Create a Web application credential in Google Cloud Console and add your local/deployed origins.
VITE_GOOGLE_CLIENT_ID=
